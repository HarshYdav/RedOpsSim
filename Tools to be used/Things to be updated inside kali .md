🔥 On Kali Linux (Attacker Machine)

Install or ensure the following:

Default Tools (Already Included)

Metasploit

Nmap

Nikto

Burpsuite (optional)

Additional Tools You NEED to install:

Run these commands later:

sudo apt update
sudo apt install python3 python3-pip
sudo apt install impacket-scripts
sudo apt install evil-winrm
sudo apt install crackmapexec


These tools enable:

Lateral movement

Credential dumping

AD enumeration

Attack simulation